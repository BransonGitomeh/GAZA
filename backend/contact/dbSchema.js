var contact = {
  identity: 'contact',
  attributes: {
    id: {
      type: 'string',
    },
    name: {
      type: 'string',
    },
    number: {
      type: 'string',
    }
  }
}

module.exports = contact;
