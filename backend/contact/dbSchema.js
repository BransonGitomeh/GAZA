var User = {
  identity: 'contact',
  attributes: {
    id: {
      type: 'string',
    },
    name: {
      type: 'string',
    },
    age: {
      type: 'string',
    }
  }
}

module.exports = User;
